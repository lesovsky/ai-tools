---
status: planned                    # planned -> in_progress -> done
depends_on: []                     # ID задач-зависимостей (строки: ["01", "02"])
wave: 1                            # волна параллельного выполнения
skills: [code-writing]             # МАССИВ скиллов для загрузки
verify:                            # инструмент верификации (опционально: curl, bash, user)
reviewers: [dev-code-reviewer, dev-security-auditor, dev-test-reviewer]  # явно указать. Пусто = fallback на defaults
teammate_name:                     # имя агента-исполнителя (опционально; если не задано — генерируется по описанию задачи)
---

# Task {ID}: Название

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](~/.claude/skills/code-writing/SKILL.md)

## Description

Что делаем и зачем. (Столько, сколько нужно для понимания — без ограничения длины.)

## What to do

Конкретные шаги — ЧТО, не КАК. Не псевдокод.

## TDD Anchor

<!-- Fill if task includes writing code. For non-code tasks (user instructions, deploy, config) — delete this section. -->

Тесты, которые нужно написать ДО реализации. Пишем → запускаем → убеждаемся что падают → пишем код → убеждаемся что проходят.

- `tests/test_api.py::test_create_user` — POST /api/users создаёт пользователя, возвращает 201
- `tests/test_api.py::test_create_user_duplicate` — POST с существующим email → 409

## Acceptance Criteria

- [ ] Критерий 1
- [ ] Критерий 2

## Context Files

**Feature artifacts:**
- [{feature_base}.md]({feature_base}.md) — user-spec
- [{feature_base}-tech-spec.md]({feature_base}-tech-spec.md) — tech-spec
- [{feature_base}-decisions.md]({feature_base}-decisions.md) — decisions log

**Project knowledge:**
- [project.md]({pk_path}/project.md)
- [architecture.md]({pk_path}/architecture.md)

**Code files:**
- [file.ts](path/to/file.ts) — что изменить

## Verification Steps

<!-- How to verify task is complete. For code — run tests. For deploy — check logs. For user-action — user confirmation. -->

- Шаг 1: что проверить
- Шаг 2: ожидаемый результат

## Details

<!-- All details for task execution — technical, organizational, any other. -->

**Files:** path/to/file.ts — что сделать
**Dependencies:** другие задачи, пакеты
**Edge cases:** список
**Implementation hints:** подсказки (НЕ псевдокод)

## Reviewers

- **dev-code-reviewer** → `{feature_base}-task-{ID}-dev-code-reviewer-review.json`
- **dev-security-auditor** → `{feature_base}-task-{ID}-dev-security-auditor-review.json`
- **dev-test-reviewer** → `{feature_base}-task-{ID}-dev-test-reviewer-review.json`

## Post-completion

- [ ] Записать краткий отчёт в [{feature_base}-decisions.md]({feature_base}-decisions.md) (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
